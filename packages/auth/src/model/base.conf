[request_definition]
r = sub, domain, data, action, method

[policy_definition]
p = sub, domain, data, action, method

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = m = g(r.sub, p.sub, r.dom) && r.dom == p.dom && r.obj == p.obj && r.act == p.act|| r.sub == "deskbtm@outlook.com"